# Impeccable Design Context — LLMTestAgent

> 本文件记录 LLMTestAgent 前端设计的上下文决策，供所有设计工作复用。
> 由 Impeccable 技能工作流维护（2026-08-19 更新为「工业蓝图」方向）。

## 产品

LLMTestAgent — AI 驱动的 API 自动化测试 Agent。用户用自然语言描述测试意图，Agent 解析 → 生成用例 → 执行 → 生成报告。

## 目标用户

- **开发 + 测试混合**（Developer + QA 工程师）
- 使用场景：日常管理测试项目、AI 生成测试用例、执行 API 测试、审阅测试报告、与 Agent 对话式协作

## 美学方向

- **「工业蓝图 / Blueprint」**（用户 2026-08-19 从 4 个可视化方案中选定）：像一张**活的工程图纸**。
- 参照气质：技术图纸 / 蓝图 / 工程标注，非市面暗色 AI 界面。
- 个性来源：
  1. **图纸网格**：全站极淡 16px 工程网格 + hero 区 24px 网格纹理（.bg-blueprint-grid）
  2. **工程蓝墨线**：单一工程蓝（oklch 0.50 0.13 255）+ 墨蓝文字，以 1px 墨线分层而非阴影
  3. **等宽标注**：mono 大写宽字距微标注（FIG.01 / PROJECT_12 / ~/dashboard），表头、状态、编号全 mono
  4. **印章式徽章**：状态/HTTP 方法为描边印章（PASS 绿印 / FAIL 红印 / 运行蓝印、pending 虚线批注）
  5. **图纸角标**：hero 卡四角十字墨线（+）
- 已否决方向（历史）：GitHub Primer 风格（原）、暗色精密 Precision Dark（上一版，被还原）

## 主题模式

- **亮色图纸为默认**（浅蓝纸底），暗色「夜图」通过 `html.dark` 类切换（深海军蓝 + 浅蓝网格）
- 切换按钮在顶栏；localStorage key `lta-theme`，缺省 light
- Shiki 代码高亮（github-light/github-dark 双主题 CSS 变量）与 Mermaid（neutral/dark）随主题联动

## 技术约束

- 保留现有技术栈：React 19 + TypeScript + Vite + Tailwind CSS v4 + shadcn/ui（Radix UI）+ TanStack React Query v5 + lucide-react + sonner + react-markdown + shiki + mermaid
- 不引入重型新依赖（无图表库，图表纯 CSS）
- 允许引入字体（Google Fonts / 自托管）

## 设计令牌摘要

- 字体：`--font-sans` Inter / `--font-mono` IBM Plex Mono / `--font-display` Space Grotesk
- 亮色图纸：background `oklch(0.955 0.012 235)` / foreground 墨蓝 `oklch(0.30 0.05 250)` / primary 工程蓝 `oklch(0.50 0.13 255)` / border 蓝墨线 `oklch(0.78 0.035 240)`
- 圆角：0 / 2 / 4 / 6px（图纸工具感，锐利）；动效：ease-out-expo，150/200/300ms
- 语义色：success 印章绿 / destructive 修订红 / warning 铅笔琥珀 / info 蓝

## 反模式（禁止）

- 纯黑/纯白、GitHub 蓝灰暗色、AI-slop（cyan+深色、紫蓝渐变、霓虹、默认深色+发光）
- 万物皆卡片 / 卡片套卡片、重阴影堆叠、大圆角（蓝图必须锐利）
- 弹跳/回弹动画、所有按钮都是主按钮
