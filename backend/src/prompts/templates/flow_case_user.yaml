prompt: |
  请根据以下API接口信息，编排一个完整的业务流程测试：

  可用接口列表：
  {endpoints_info}

  要求：
  1. 分析以上接口的业务关系，编排合理的调用顺序
  2. 确定接口间的数据依赖（哪个接口的响应数据需要传给下一个接口）
  3. 为每个步骤配置正确的 cache_rules
  4. 第一个步骤的 inject 应为空（无前置依赖）
  5. 每个步骤都需要合理的断言规则
  6. 输出有效的 JSON 格式
